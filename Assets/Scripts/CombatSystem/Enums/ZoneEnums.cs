using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CombatSystem.Enums
{
    public enum ZoneDirection { Front, Right, Back, Left }
    public enum ZoneDistance { Near, Mid, Far }
    public enum StepType { None, Left, Right, Forward, Backward }
    public enum ActionType { None, HalfAttack, FullAttack, Dodge, Block, Special, Step }
}


